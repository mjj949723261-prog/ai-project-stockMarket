# 共享产品协议

该目录用于约束三端一致性，避免 `Vue3`、`SwiftUI`、`Flutter` 在功能和信息结构上跑偏。

包含内容：

- `stock-schema.json`：统一数据结构
- `stock-fixtures.json`：统一 mock 数据
- `scoring-rules.md`：评分规则说明
- `component-contracts.md`：组件职责说明

